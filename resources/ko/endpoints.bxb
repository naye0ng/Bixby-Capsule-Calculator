// models와 JS를 연결한다.
endpoints {
  action-endpoints {
    action-endpoint (Calculator) {
      // action/Calculator.model.bxb에서 정의한 input 변수 이름
      accepted-inputs (leftOperand, rightOperand, operator)
      local-endpoint (calculator.js)
    }
  }
}
